rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The current voice ledger and tester allowlist are server/admin-only.
    match /{document=**} { allow read, write: if false; }
  }
}
